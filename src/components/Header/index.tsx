import React from 'react';
import { Flex } from '../../ui-kit/blocks';

const Header = () => {
    const a = 'Hello';

    return (
        <Flex bg={'red'} asd="asd">
            {a}
        </Flex>
    );
};

export default Header;
